//!
//! # Library backend
//!
//! This handles the logic for the cli tool `rost_sessionizer`.

/// Module that handles the logic
pub mod commands;

/// Module that handles the configuration.
pub mod config;

/// Module that provides internally used functions
pub mod utils;
