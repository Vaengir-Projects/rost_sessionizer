When using 'startup' command in bashrc a session named '0' is started as well.

Bug wasn't actually caused by this program, but rather my own local setup having some leftovers from tmux-resurrect
and tmux-continuum.
