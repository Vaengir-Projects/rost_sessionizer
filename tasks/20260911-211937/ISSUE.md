I want this program to have a flag that makes it close the current active session and one to close all open sessions.
This should respect the 'Default' session, which means it returns to this session after closing another and it won't close this session.
